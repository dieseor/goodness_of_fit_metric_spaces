# .Rprofile for the goodness_of_fit_metric_spaces project
# This file is executed when R starts in this directory

# Set project-specific options
options(
  digits = 4,
  scipen = 999,  # Avoid scientific notation
  width = 120    # Console width
)

# Note: To install required packages, run in interactive R:
# install.packages(c("ggplot2", "dplyr", "readr", "here"))